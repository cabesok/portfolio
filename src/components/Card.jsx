import React from 'react'

const Card = (props) => {

  return (
    <></>

   /*  <article className='card' onClick={handleTouch} >
                    <img src={props.image} ></img>
                    <div className={`layer ${active ? `active${props.id}` : ''}`}>
                        <h3> {props.name} </h3>
                        <p className='descriptionproject' > {props.description} </p>
                        <p className='technologies' > {props.technologies}</p>
                        <div className="links">
                            <a href={props.link} target='_blank'> <i class="fa-sharp fa-solid fa-arrow-up-right-from-square"></i> </a>
                            <a href={props.github} target='_blank'> <i class="fa-brands fa-github"></i> </a>
                        </div>
                    </div>
                </article> */
  )

}

export default Card